/**
 * Frontend-local types. Domain types are re-exported from the shared package
 * so components import everything from one place.
 */
export type {
  Product,
  Category,
  ApiResponse,
  ApiErrorResponse,
  ApiResult,
  HealthStatus,
} from '@security-system-builder/shared';
